apiVersion: v1
kind: Service
metadata:
  name: agenticquote
  namespace: production
  labels:
    app: agenticquote
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: agenticquote
